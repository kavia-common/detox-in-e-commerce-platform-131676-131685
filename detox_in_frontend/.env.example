# PUBLIC_INTERFACE
# DETOX-IN Frontend environment variables
# Copy to .env or .env.local and adjust values as necessary.

# Base URL for the backend API (default is '/api' which works when frontend is reverse proxied)
REACT_APP_API_BASE=/api
